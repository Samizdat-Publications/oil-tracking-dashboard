# API keys
backend/.env
.env

# Python
__pycache__/
*.pyc
backend/data/cache.db

# Node
node_modules/
frontend/dist/
frontend/dist-ssr/

# Claude Code
.claude/

# Brainstorm mockups
.superpowers/

# Editor
.vscode/
.idea/
*.sw?

# Design-tool exports and scratch archives. These are large binary drops from
# Claude Design / other tools, not source. Keep them locally; the briefs in
# docs/design-briefs/ are the durable artefact.
*.zip
Oil Dashboard New Design/

# Wrangler local cache. Holds the resolved account id and name (derived from the
# account email) -- not a credential, but local state rather than source.
.wrangler/
